import 'package:flutter/material.dart';

class LineOfActors extends StatelessWidget {
  const LineOfActors({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}